#include <iostream>
#include <fstream>
#include <boost/functional/hash.hpp>
#include <Eigen/Dense>
#include <vector>
#include <unordered_map>
#include <random>
#include <string>
#include <thread>

using namespace std;
using namespace Eigen;

// std::vector<std::vector<float>> refer(100, std::vector<float>(100, 1));
// std::vector<std::vector<float>> real(100, std::vector<float>(100, 1));

// vector<vector<float>> matrix1(3,vector<float> (3,0));
// vector<vector<float>> matrix2(3,vector<float> (3,0));
Matrix3i a;

int main()
{ 
    // vector<int> a={1,2,3,4,5,6,7,8,9};
    // vector<int> b;
    // b.assign(a.begin()+2,a.begin()+6);


    // for(int i=0;i<b.size();i++){
    //     cout<<b[i]<<'\t';
    // }
    // cout<<'\n';

    // b.assign(a.begin()+4,a.begin()+7);

    // for(int i=0;i<b.size();i++){
    //     cout<<b[i]<<'\t';
    // }
    // cout<<'\n';    
}